/**
 * Placeholder export for Session 0.
 * Real design tokens (color, spacing, type scale) land in a later session.
 * Deliberately no colors/fonts here yet.
 */
export const THEME_PACKAGE_NAME = '@tpa/theme' as const;
