import { PageHeader } from '../ui';

/** Coaches route. Coach cards + edit land in S4e. */
export function Coaches() {
  return (
    <PageHeader
      eyebrow="Team"
      title="Coaches"
      subtitle="The people who run every session on court. Availability templates and one-off slots are assigned to these coaches."
    />
  );
}
